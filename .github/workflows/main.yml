name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      - name: Unzip Project
        run: unzip GVCW_OTP-App.zip

      - name: Build APK
        run: |
          cd GVCW_OTP-App
          gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: GVCW-OTP-Relay-APK
          path: GVCW_OTP-App/app/build/outputs/apk/debug/app-debug.apk
